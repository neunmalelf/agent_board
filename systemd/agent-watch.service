[Unit]
Description=agent-watch: notify which agent tab needs input
After=graphical-session.target

[Service]
Type=simple
Environment=PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin
ExecStart=%h/projects/agent-watcher/agent-watch
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
